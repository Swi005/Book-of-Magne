program Proc_Example 
begin
    procedure swap (upd x: integer, upd y:integer)
    begin
        var tmp : integer; //
        tmp := x;
        x := y;
        y := tmp;
    end

    procedure main ()
    begin
        var a = 4;
        var b = 5;
        call tmp(4,5);
    end
end