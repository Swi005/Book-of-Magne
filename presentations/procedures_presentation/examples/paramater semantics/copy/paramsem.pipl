procedure GroupSwap (upd x: integer, upd y :integer) 
begin
    y := x + y;
    x := y - x;
    y := y - x;
end; 

procedure SelfSwap();
begin
    var a = 5;
    call GroupSwap (a, a); //x =a, y = a
end;